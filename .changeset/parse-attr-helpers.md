---
"@agencecinq/utils": minor
"@agencecinq/combobox": patch
"@agencecinq/accordion": patch
"@agencecinq/spinbutton": patch
---

Add `parseNumber` and `parseBoolean` helpers alongside `parseList` in a single `parse` module, and use them in combobox, accordion, and spinbutton.
