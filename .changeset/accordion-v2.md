---
"@agencecinq/accordion": major
---

Breaking: rename public types to `Options` / `Detail` and export `Panel`. Fix URL hash matching for `#id` (and `#/id`). Cancel panel animation timers on destroy. Delegate panel-open handling on the host.
