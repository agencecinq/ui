---
"@agencecinq/calendar": major
"@agencecinq/docs": patch
---

Replace boolean `single` with `mode="single|range|multiple"` on `<cinq-calendar>`. Document and demo multiple dates in the docs playground.
