---
"@agencecinq/calendar": major
"@agencecinq/utils": patch
"@agencecinq/docs": patch
---

Add `@agencecinq/calendar` with `<cinq-calendar>` Web Component for single-date and range selection (ported from `@19h47/calendar`). Export `CALENDAR_CHANGE` from `@agencecinq/utils`. Add calendar docs page and live playgrounds.
