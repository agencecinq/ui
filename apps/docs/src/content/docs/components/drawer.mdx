---
title: Drawer
description: Slide-in drawer Web Component (`<cinq-drawer>`).
---

import PortalToBody from "../_components/PortalToBody.astro";
import DrawerPlayground from "../_components/DrawerPlayground.astro";
import DrawerExclusivePlayground from "../_components/DrawerExclusivePlayground.astro";
import DrawerMultiButtonPlayground from "../_components/DrawerMultiButtonPlayground.astro";

A drawer slides a panel in from the edge of the viewport. `<cinq-drawer>` wraps
your overlay and panel markup, handles focus trap, scroll lock, and open/close
events. `<cinq-drawer-button>` connects triggers through `aria-controls`.

## Install

```bash
pnpm add @agencecinq/drawer
```

## Usage

Import once:

```js
import "@agencecinq/drawer";
```

Then write the markup — overlay, panel (`role="dialog"`), and buttons with
matching `aria-controls`. The component adds interactivity only.

> **HTML is the source of truth.** The component will not auto-set `role`,
> auto-migrate attributes, or warn about missing labels. Run an a11y linter (axe,
> Lighthouse) to catch invalid markup.

```html
<cinq-drawer-button class="block">
  <button aria-controls="demo-drawer">Open drawer</button>
</cinq-drawer-button>

<cinq-drawer id="demo-drawer" class="group">
  <div data-dom="overlay" class="fixed inset-0 z-[2147483646] bg-black/50 opacity-0 pointer-events-none group-[[open]]:opacity-100 group-[[open]]:pointer-events-auto"></div>
  <div role="dialog" class="fixed right-0 top-0 z-[2147483647] h-full w-full max-w-md bg-white shadow-2xl translate-x-full transition-transform group-[[open]]:translate-x-0">
    <div class="p-8 flex flex-col gap-4">
      <h2 class="text-2xl font-bold">Bestiary drawer</h2>
      <p class="text-gray-600">
        Use the drawer as a sliding panel listing creatures from your AD&amp;D bestiary: trolls,
        dracoliches, or galeb duhrs guarding the dungeon entrance.
      </p>
      <cinq-drawer-button class="block">
        <button aria-controls="demo-drawer">Close</button>
      </cinq-drawer-button>
    </div>
  </div>
</cinq-drawer>
```

## API

### Attributes

| Attribute | Required | Description                                      |
| --------- | -------- | ------------------------------------------------ |
| `id`      | Yes      | Drawer identifier; must match button `aria-controls`. |
| `open`    | No       | Reflected state attribute (useful for styling). |

### Methods

| Method     | Description                         |
| ---------- | ----------------------------------- |
| `open()`   | Opens the drawer.                  |
| `close()`  | Closes the drawer.                 |
| `toggle()` | Toggles between open and closed.   |

### Events

| Event           | Dispatched by         | Detail payload                      | Description                                                   |
| --------------- | --------------------- | ----------------------------------- | ------------------------------------------------------------- |
| `drawer-toggle` | `cinq-drawer-button`  | `{ drawer, trigger, trap }`        | Emitted on `document.documentElement` to request a toggle.    |
| `drawer-open`   | `cinq-drawer`         | `{ drawer, trigger }`              | Fired when a drawer starts opening. Other open drawers close themselves. |
| `drawer-close`  | `cinq-drawer`         | `{ drawer }`                       | Fired when a drawer starts closing.                                   |

## Shopify (Liquid snippet)

If you are integrating in a Shopify theme, you can use the provided snippet and Vite plugin.

### 1) Copy the snippet automatically (Vite)

```ts
import { defineConfig } from 'vite';
import { cinqDrawerPlugin } from '@agencecinq/drawer/plugin';

export default defineConfig({
  plugins: [cinqDrawerPlugin()],
});
```

This copies the package snippet to your theme:

- `snippets/cinq-drawer.html.liquid`

### 2) Render the snippet

```liquid
{% render 'cinq-drawer.html',
  id: 'bestiary-drawer',
  content: '<div class="p-8"><p class="mb-2">The galeb duhr looks like a boulder until it moves.</p><p class="mb-0">Trolls and jermlaines skulk deeper in the dungeon corridors.</p></div>'
%}

<cinq-drawer-button class="block">
  <button aria-controls="bestiary-drawer" aria-expanded="false">Open bestiary</button>
</cinq-drawer-button>
```

## Interactive example

<div class="not-content">
  <div class="flex flex-col gap-4">
    <cinq-drawer-button class="block">
      <button
        class="px-4 py-2 rounded-lg bg-black text-white hover:bg-gray-800"
        aria-controls="starlight-drawer"
        aria-expanded="false"
      >
        Open drawer
      </button>
    </cinq-drawer-button>

    <cinq-drawer id="starlight-drawer" class="group">
      <div
        data-dom="overlay"
        class="fixed inset-0 z-[2147483646] bg-black/50 opacity-0 pointer-events-none transition-opacity group-[[open]]:opacity-100 group-[[open]]:pointer-events-auto"
      ></div>
      <div
        role="dialog"
        class="fixed right-0 top-0 z-[2147483647] h-full w-full max-w-md bg-white shadow-2xl translate-x-full transition-transform duration-300 group-[[open]]:translate-x-0"
      >
        <div class="p-8 flex flex-col gap-4">
          <h2 class="text-2xl font-bold">CINQ drawer — encounter prep</h2>
          <p class="text-gray-600">
            Keep quick notes on the next encounter: a troll ambush, a cloaker in the rafters, or a
            nest of jermlaines sabotaging the party.
          </p>
          <cinq-drawer-button class="block">
            <button
              class="px-4 py-2 rounded-lg bg-black text-white hover:bg-gray-800"
              aria-controls="starlight-drawer"
              aria-expanded="false"
            >
              Close
            </button>
          </cinq-drawer-button>
        </div>
      </div>
    </cinq-drawer>
  </div>
</div>

<PortalToBody selector="#starlight-drawer" />

## Playground (live)

<DrawerPlayground />

### Exclusive drawers

Only one drawer stays open at a time. A shared return-focus target remembers the
page control that started the chain (triggers inside a drawer are ignored). Open
notes → switch to bestiary → close: focus lands back on **Open left**.

<DrawerExclusivePlayground />

### Multiple buttons, one drawer

Several triggers can share the same `aria-controls` id. Each
`<cinq-drawer-button>` listens for `drawer-open` / `drawer-close` and updates its
own `aria-expanded` — open or close from any control and they all stay in sync
(red ring when collapsed, green when expanded).

<DrawerMultiButtonPlayground />

