---
title: CINQ UI components
description: Documentation and interactive examples for CINQ UI Web Components.
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import RegisterComponents from './_components/RegisterComponents.astro';

## Components

<CardGrid stagger>
  <Card title="Tabs" icon="list">
    Accessible, WAI-ARIA compliant tabs as a lightweight Web Component.

    [Read the Tabs docs](components/tabs/)
  </Card>
  <Card title="Modal" icon="window">
    Native `<dialog>`-based modal with a tiny event-driven API.

    [Read the Modal docs](components/modal/)
  </Card>
  <Card title="Drawer" icon="sidebar">
    Slide-in drawer with focus trap and scroll lock.

    [Read the Drawer docs](components/drawer/)
  </Card>
  <Card title="Spinbutton" icon="up-and-down-arrows">
    Accessible numeric stepper with keyboard support and live-region announcements.

    [Read the Spinbutton docs](components/spinbutton/)
  </Card>
  <Card title="Disclosure Button" icon="open-book">
    Controller for `aria-expanded` triggers that show or hide controlled regions via `<cinq-disclosure-button>`.

    [Read the Disclosure Button docs](components/disclosure-button/)
  </Card>
  <Card title="Switch" icon="approve-check">
    Binary on/off control following the WAI-ARIA switch pattern via `<cinq-switch>`.

    [Read the Switch docs](components/switch/)
  </Card>
  <Card title="Accordion" icon="list-format">
    Expandable sections with keyboard navigation via `<cinq-accordion>`.

    [Read the Accordion docs](components/accordion/)
  </Card>
  <Card title="Combobox" icon="magnifier">
    Editable list autocomplete following the WAI-ARIA combobox pattern via `<cinq-combobox>`.

    [Read the Combobox docs](components/combobox/)
  </Card>
  <Card title="Window Splitter" icon="setting">
    Resizable panes with a focusable separator via `<cinq-windowsplitter>`.

    [Read the Window Splitter docs](components/windowsplitter/)
  </Card>
</CardGrid>

## Philosophy

- **Web Components first**: import once, drop the markup, the browser instantiates it.
- **HTML is the source of truth**: you write the markup — native `<dialog>`, `<input>`, `role="tab"`, `aria-expanded`, and the rest. Each package wires interactivity to that DOM; it extends HTML, it does not generate structure or silently replace semantics.
- **Event-driven**: integrate with your app by listening to events and dispatching intents.
- **WAI‑ARIA aligned**: keyboard navigation and ARIA states follow APG patterns.

## Conventions

- **`$` prefix**: a property or local named with a leading `$` (e.g. `$button`, `$input`) holds a DOM node — an `HTMLElement` (or a more specific subtype such as `HTMLButtonElement`). Plain names are reserved for values, IDs, and non-DOM state.

Interactive examples use **AD&D 2nd Edition** flavour — ability scores, spell slots, bestiary entries, and dungeon delves — so demos read like a campaign sheet rather than a generic web app.

<RegisterComponents />
